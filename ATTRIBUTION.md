# Attribution

nix-shebang is pure Nix with no vendored code or runtime services. Its
only dependencies are the flake inputs in [`flake.nix`](flake.nix)
(nixpkgs via nixpkgs-lock, nix-unit, set-and-setting) -- all permissive.
See `flake.nix` for the authoritative list.
